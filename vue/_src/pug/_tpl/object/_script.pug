block js
  if userType == 'user'
    script(src="/_assets/js/app-user.js")
  if userType == 'admin'
    script(src="/_assets/js/app-admin.js")

script
  .
    if (location.pathname === '/') {
      location.href = '/user/';
    }
